/**
 * Database type stub. Will be regenerated from Supabase with:
 *   npx supabase gen types typescript --project-id lbvboqoyvuxuanwvtypf > src/lib/supabase/types.ts
 *
 * For now we keep a permissive shape so the client builds without
 * coupling to the schema. Replace this once the migration is applied
 * and we run the codegen.
 */
export type Database = Record<string, unknown>;
